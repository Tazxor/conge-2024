{% extends 'base.html.twig' %}

{% block title %}Label{% endblock %}

{% block body %}
    <h1>Label</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ label.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ label.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_label_index') }}">back to list</a>

    <a href="{{ path('app_label_edit', {'id': label.id}) }}">edit</a>

    {{ include('label/_delete_form.html.twig') }}
{% endblock %}
